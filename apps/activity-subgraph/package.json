{
  "name": "@repo/activity-subgraph",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "build:mezo-testnet": "graph build testnet/subgraph.yaml",
    "deploy:mezo": "goldsky subgraph deploy matchbox-explorer/3.1.1 --path .",
    "deploy:mezo-testnet": "pnpm run build:mezo-testnet && goldsky subgraph deploy matchbox-explorer-testnet/3.1.1 --path .",
    "deploy:mainnet": "pnpm run deploy:mezo",
    "deploy:testnet": "pnpm run deploy:mezo-testnet"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.37.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
