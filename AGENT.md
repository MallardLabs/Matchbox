# AGENT.md - Agent Instructions

Follow the instructions in `./CLAUDE.md`.

Always also respect the MatchBox-specific repo guidance in:

- `./agent-instructions/matchbox-repo-roles.md`

This repo-role guidance is intentionally specific to `MallardLabs/MatchBox` and
should be absent from `TrelliumOrg/MatchBox`.

Before any Trellium release promotion, run `pnpm check:trellium` against the
release candidate or staging tree.
